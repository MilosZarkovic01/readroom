services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: readroom
      POSTGRES_PASSWORD: readroom
      POSTGRES_DB: readroom
    volumes:
      - readroom_pg:/var/lib/postgresql/data

volumes:
  readroom_pg:
