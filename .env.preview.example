# Vercel Preview / STG — do not copy Production values here.
# These keys are set on the Vercel Preview environment, not committed.

DATABASE_URL="postgresql://stg-user:stg-pass@stg-host-pooler/neondb?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://stg-user:stg-pass@stg-host/neondb?sslmode=require"
AUTH_SECRET="preview-only-secret-at-least-32-characters"
AUTH_TRUST_HOST="true"
READROOM_ENV="stg"
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

SMTP_HOST="smtp.ethereal.email"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="stg-ethereal-user"
SMTP_PASS="stg-ethereal-pass"
EMAIL_FROM="ReadRoom STG <stg@ethereal.email>"
